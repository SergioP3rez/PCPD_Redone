# PCPD_Redone
This repository contains the source code of the pCenter-pDispersion problem. To reproduce the experiments, you only should include the GrafoOptLib.jar as external library in your project, change the path where you have stored the instances and execute the main method included in ExperimentsAlgorithm class.
